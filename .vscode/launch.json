{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run C program (sem lixo no terminal)",
            "type": "cppvsdbg",
            "request": "launch",
            "program": "${fileDirname}\\${fileBasenameNoExtension}.exe",
            "cwd": "${fileDirname}",
            "console": "integratedTerminal"
        }
    ]
}
